name: Render
on:
  workflow_call:

jobs:
  render:
    name: Test
    runs-on: self-hosted

    steps:
      - name: Test
        run: |
          echo ${{ secrets.SECRET1 }}
          echo ${{ secrets.SECRET2 }}
